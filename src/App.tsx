import React from 'react'
import { ErrorBoundary } from './components/ErrorBoundary'

/**
 * Renders the Application layout in this application
 */
const App = () => {
  return <ErrorBoundary>Template</ErrorBoundary>
}

export default App
